/**
 * Optional, lightweight rule-style decision tree utilities.
 *
 * <p>The package has no global registry or automatic runtime hook. Modules create trees explicitly and call
 * evaluate only when they need a decision.</p>
 */
package com.y271727uy.shopcore.core.algorithm.decisiontree;
